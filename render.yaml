services:
  - type: web
    name: senai-testes-sistema
    runtime: docker
    plan: free
    healthCheckPath: /actuator/health
    envVars:
      - key: APP_CORS_ALLOWED_ORIGIN_PATTERNS
        value: http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001,https://*.onrender.com
